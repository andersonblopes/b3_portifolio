[tool.black]
line-length = 100
target-version = ["py39"]
skip-string-normalization = false

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
# Keep it pragmatic: catch real bugs + enforce clean imports
select = [
  "E",   # pycodestyle errors
  "F",   # pyflakes
  "I",   # isort
  "UP",  # pyupgrade
  "B",   # bugbear
]
ignore = [
  # Streamlit apps often use top-level imports/side effects; keep noise low initially.
]

[tool.ruff.lint.per-file-ignores]
# i18n string dict: long translated UI copy is expected and splitting it
# hurts readability/diffability far more than it helps.
"src/langs.py" = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["src"]

[tool.pytest.ini_options]
addopts = "-q"

[tool.coverage.run]
# app.py is the Streamlit entrypoint — not unit-testable without a running server
omit = ["src/app.py"]

[tool.coverage.report]
fail_under = 90
